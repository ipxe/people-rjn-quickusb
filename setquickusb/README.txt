setquickusb sets the I/O mask for the QUSB device - on the various ports.

To compile/install, do;  make && sudo make install

Invoke with -h  for help


Contents:
	setquickusb.c				- The C program to control QUSB ioctls

	Makefile  				- Makefile

	README.txt  				- This file

	setquickusb.bashcompletion  		- If placed in /etc/bash_completion.d, this will enable bash-completion to understand some of the setquickusb syntax.
	
	man/					- manpages
